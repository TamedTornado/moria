# Issue 228 — Wire acquired assets/config/curated_manifest.ron

References: `docs/tdd/assets.md §Curated manifest; §Production asset registries; docs/tdd/data-model.md §Seed, region, and generated metadata`.

## Boundary contracts

- After exact production region config and ruin stamp are installed, regenerate CuratedManifest through the public curation facade and require byte-for-byte equality plus matching parameters_digest before installing only `assets/config/curated_manifest.ron`.
- Both production registries must match its stable ID/path/content digest/provenance/budget, and runtime validation must pass every complete-route, object/index/disjointness/signature contract.

## Multi-system scenarios

- Valid generated bytes open through normal public startup with no fallback and expose the complete route.
- Change config, stamp, generated manifest byte, collection order, identity/digest, route feature, object/index cap or overlap witness independently; regeneration/runtime validation must fail deterministically.
- Simulate stale assisted output after authoritative inputs change and concurrent asset completion orders.

## Failure propagation

- Any byte/digest/registry/schema/complete-route mismatch blocks readiness and must not install stale output or construct an accessible store/index.
- Generator/validator errors remain typed; no hand-edited or partially valid manifest is accepted.

## Ordering guarantees

- Config+stamp validation/hash -> deterministic regeneration -> byte/digest comparison -> registry/content validation -> runtime manifest/index validation -> WorldReady.

## Conformance-harness hook

Run through issue 232's public startup/failure-injection adapter and preserve this registry/content failure as a deterministic fixture.

